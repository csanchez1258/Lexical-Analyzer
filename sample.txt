[* this is comment for this sample code which
        converts liters to ounces *]

function convertL (lit int)
{
            return (lit * 33.814)
}

#
    int conversion; [*declarations*]
#

put(convertL(conversion));
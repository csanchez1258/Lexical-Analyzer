[*this code should get the grade and add it to the total and get the mean*]
function averageNum(mean1 int)
{
    sum = 34 + 100 + 43 + 99 + 78 + 89;
    avg = (sum + mean1)/ 7;
    return avg;
}

#
    int grade, boost, curve; 
#

get(grade, average);
if (grade < curve)
{
    put (grade);
    put (averageNum(grade)); [*this should add grade to the function*]
}
fi;
if (grade < curve)
{
    return 0;
}
fi;